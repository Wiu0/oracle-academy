import java.util.Scanner;

public class MediaAlunoMetodo {
	
	public static void verificarSeNotaEhValidaOuInvalida(double nota) {
		if(nota >= 0 && nota <= 10) {
			System.out.println("Nota valida");
		}else {
			System.out.println("Nota invalida");
		}
	}
	
	public static void calcularMedia(double arroz, double feijao, double carne, double nota4) {
		double media = (arroz + feijao + carne + nota4)/4;
		System.out.println("média de " + media);
	}

	public static void main(String[] args) {
		//Nota deve ser maior/igual 0 e menor/igual 10
		
		String nome = "Wiu";
		Scanner sc = new Scanner(System.in);
//		double nota4;
		double nota1 = sc.nextDouble(), nota2 = sc.nextDouble(), nota3 = sc.nextDouble(), nota4 = sc.nextDouble();
		
	
		verificarSeNotaEhValidaOuInvalida(nota1);//nota1
		verificarSeNotaEhValidaOuInvalida(nota2);//nota1
		verificarSeNotaEhValidaOuInvalida(nota3);//nota3
		verificarSeNotaEhValidaOuInvalida(nota4);//nota4
		
		calcularMedia(nota1, nota2, nota3, nota4);
		
		calcularMedia(12, 45, 19, 20);
		calcularMedia(9, 13, 1, 8);
		
		sc.close();
	
	}
}